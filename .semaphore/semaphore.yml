version: v1.0
name: Python Test Pipeline

agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2204

blocks:
  - name: Run tests
    task:
      jobs:
        - name: Test Job
          commands:
            - checkout
            - python -m pip install -r requirements.txt
            - python -m pytest
